name: Checkmarx One Vscode Extension CI Linux Test cases

on: [pull_request]

jobs:
  # Test the shared core package independently
  core-unit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.0
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 22.11.0
      - name: Authenticate with GitHub package registry
        run: echo "//npm.pkg.github.com/:_authToken=${{ secrets.GH_TOKEN }}" > ~/.npmrc
      - name: Install core dependencies
        run: cd packages/core && npm install
      - name: Run core unit tests with coverage
        run: cd packages/core && npm run unit-coverage
      - name: Upload core coverage report
        uses: actions/upload-artifact@c7d193f32edcb7bfad88892161225aeda64e9392 #v4
        with:
          name: coverage-core
          path: packages/core/coverage/
          retention-days: 7

  # Test both extensions with E2E tests
  end-to-end-tests:
    strategy:
      max-parallel: 1
      matrix:
        extension: [checkmarx, project-ignite]
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.0
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 22.11.0
      - name: Authenticate with GitHub package registry
        run: echo "//npm.pkg.github.com/:_authToken=${{ secrets.GH_TOKEN }}" > ~/.npmrc
      - name: Install dependencies
        run: |
          cd packages/core && npm install
          cd ../checkmarx && npm install
          cd ../project-ignite && npm install
      - name: Build core package
        run: cd packages/core && npm run compile
      - name: Build extension
        run: cd packages/${{ matrix.extension }} && npm run compile
      - name: Run e2e tests
        if: runner.os == 'Linux'
        env:
          CX_APIKEY: ${{ secrets.CX_APIKEY_E2E_DEU }}
        run: |
          sudo apt-get install xvfb
          export DISPLAY=:99.0
          Xvfb -ac :99 -screen 0 1920x1080x16 &
          cd packages/core && npm run test:ui-end-to-end
      - name: Upload test artifacts
        if: ${{ failure() }}
        uses: actions/upload-artifact@c7d193f32edcb7bfad88892161225aeda64e9392 #v4
        with:
          name: e2e-artifacts-${{ matrix.extension }}-${{ matrix.os }}
          path: |
            packages/core/test-resources/screenshots/*.png
          retention-days: 2
  # Run unit tests for both extensions (note: core tests run in separate job)
  unit-tests:
    strategy:
      max-parallel: 1
      matrix:
        extension: [checkmarx, project-ignite]
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.0
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 22.11.0
      - name: Authenticate with GitHub package registry
        run: echo "//npm.pkg.github.com/:_authToken=${{ secrets.GH_TOKEN }}" > ~/.npmrc
      - name: Install dependencies
        run: |
          cd packages/core && npm install
          cd ../${{ matrix.extension }} && npm install
      - name: Build core package
        run: cd packages/core && npm run compile
      - name: Build extension
        run: cd packages/${{ matrix.extension }} && npm run compile
      - name: Run extension unit tests with coverage
        run: |
          cd packages/core && npm run unit-coverage
      - name: Upload extension coverage report
        uses: actions/upload-artifact@c7d193f32edcb7bfad88892161225aeda64e9392 #v4
        with:
          name: coverage-${{ matrix.extension }}
          path: packages/core/coverage/
          retention-days: 7

  # Run integration tests for both extensions
  ui-tests:
    strategy:
      max-parallel: 1
      matrix:
        extension: [checkmarx, project-ignite]
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.0
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 22.11.0
      - name: Authenticate with GitHub package registry
        run: echo "//npm.pkg.github.com/:_authToken=${{ secrets.GH_TOKEN }}" > ~/.npmrc
      - name: Install dependencies
        run: |
          cd packages/core && npm install
          cd ../${{ matrix.extension }} && npm install
      - name: Build core package
        run: cd packages/core && npm run compile
      - name: Build extension
        run: cd packages/${{ matrix.extension }} && npm run compile
      - name: Run integration tests (non-Linux)
        if: runner.os != 'Linux'
        env:
          CX_API_KEY: ${{ secrets.API_KEY }}
          CX_TEST_SCAN_ID: ${{ secrets.TEST_SCAN_ID }}
        run: |
          cd packages/core && npm test
      - name: Run integration tests (Linux)
        if: runner.os == 'Linux'
        env:
          CX_API_KEY: ${{ secrets.API_KEY }}
          CX_TEST_SCAN_ID: ${{ secrets.TEST_SCAN_ID }}
        run: |
          sudo apt-get install xvfb
          export DISPLAY=:99.0
          Xvfb -ac :99 -screen 0 1920x1080x16 &
          cd packages/core && npm test
      - name: Upload test artifacts
        if: ${{ failure() }}
        uses: actions/upload-artifact@c7d193f32edcb7bfad88892161225aeda64e9392 #v4
        with:
          name: integration-artifacts-${{ matrix.extension }}-${{ matrix.os }}
          path: |
            packages/core/test-resources/screenshots/*.png
          retention-days: 2
      - name: Upload screenshots
        uses: actions/upload-artifact@c7d193f32edcb7bfad88892161225aeda64e9392 #v4
        if: failure() && runner.os == 'macOS'
        with:
          name: screenshots-${{ matrix.extension }}
          path: /Users/runner/work/ast-vscode-extension/ast-vscode-extension/packages/core/test-resources/screenshots/*.png
